{
  "name": "advanced-solar-tech-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Advanced Solar Tech — component-based, data-driven marketing site.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/emit-redirects.mjs && node scripts/copy-cms.mjs",
    "build:staging": "node scripts/build-staging.mjs",
    "check:staging": "node scripts/check-html.mjs --base=/new",
    "preview": "astro preview",
    "check:content": "node scripts/check-content.mjs",
    "check:html": "node scripts/check-html.mjs",
    "verify": "npm run build && npm run check:html && npm run check:content",
    "reviews": "node scripts/fetch-google-reviews.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "@sveltia/cms": "^0.179.0",
    "astro": "^7.1.6"
  }
}
